
#include <stdio.h>
#include <windows.h>

int main()
{
    int i, j, tamaño, vector[50];


      system("cls");
      system("color D0");
      printf("Programa con vector 50, donde se elige el tamaño\n");
	  printf("Teclea el tamaño deseado max 50\n")
        scanf("%d", & vector);
        for(i=1; i= tamaño; i++)
            {

        scanf("%d", &tamaño[i]);
        }
        for(j=1; i= tamaño; j++)

        scanf("%d", &tamaño [j]);
      }
